# Exploratory Scripts

These scripts were used during investigation, calibration checks, visual
inspection, and earlier ceiling/opening experiments. They are retained for
traceability but are not part of the final reproducible submission path.

Use the root-level scripts for the final workflow:

```powershell
uv run python scripts/finalize_pipeline.py
uv run python scripts/package_submission.py
```
